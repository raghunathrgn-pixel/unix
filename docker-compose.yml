version: '3'

services:
  mongo:
    image: mongo
    ports:
      - "27017:27017"

  backend:
    build: .
    ports:
      - "5000:5000"
    environment:
      - MONGO_URI=mongodb://mongo:27017/moneydb
    depends_on:
      - mongo